# 전체 레이아웃 구조

## 1. 메인 레이아웃

```
┌─────────────────────────────────────────────────────────────────┐
│                    포트폴리오 대시보드                             │
│                                                                 │
│  ┌──────────┐  ┌────────────────────────────────────────────┐  │
│  │          │  │  📅 분석 월 선택: [2025-12 ▼]              │  │
│  │ 사이드바  │  │                                            │  │
│  │          │  │  ┌──────────────────────────────────────┐  │  │
│  │ 페이지    │  │  │                                      │  │  │
│  │ 선택:    │  │  │                                      │  │  │
│  │          │  │  │         메인 콘텐츠 영역              │  │  │
│  │ • 월별   │  │  │                                      │  │  │
│  │   투자   │  │  │    (선택한 페이지에 따라 변경)        │  │  │
│  │   비교   │  │  │                                      │  │  │
│  │          │  │  │                                      │  │  │
│  │ • 계좌별 │  │  │                                      │  │  │
│  │   포트   │  │  │                                      │  │  │
│  │   폴리오 │  │  │                                      │  │  │
│  │          │  │  │                                      │  │  │
│  │ • 전체   │  │  └──────────────────────────────────────┘  │  │
│  │   포트   │  │                                            │  │
│  │   폴리오 │  │                                            │  │
│  │          │  │                                            │  │
│  └──────────┘  └────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 2. 공통 컴포넌트

### 2.1 헤더 영역
- **분석 월 선택 드롭다운**:
  - 위치: 메인 콘텐츠 상단
  - 컴포넌트: `st.selectbox`
  - 데이터 소스: `months` 테이블의 `year_month`
  - 상태 관리: `st.session_state.selected_month`

### 2.2 사이드바
- **페이지 네비게이션**:
  - 위치: 좌측 고정
  - 컴포넌트: `st.sidebar.radio` 또는 `st.sidebar.selectbox`
  - 옵션:
    1. 월별 투자 비교
    2. 계좌별 포트폴리오
    3. 전체 포트폴리오

### 2.3 상태 관리
```python
# 세션 상태 초기화
if 'selected_month' not in st.session_state:
    st.session_state.selected_month = get_latest_month()

if 'selected_page' not in st.session_state:
    st.session_state.selected_page = "월별 투자 비교"
```

## 3. 색상 팔레트

### 3.1 자산 유형별 색상
- **STOCK (주식)**: `#3498db` (파란색)
- **BOND (채권)**: `#2ecc71` (초록색)
- **CASH (현금)**: `#f39c12` (주황색)

### 3.2 차트 색상
- **증가 (positive)**: `#27ae60` (녹색)
- **감소 (negative)**: `#e74c3c` (빨간색)
- **중립 (neutral)**: `#95a5a6` (회색)

## 4. 타이포그래피

### 4.1 텍스트 크기
- **페이지 타이틀**: `st.title()` - 가장 큰 헤더
- **섹션 헤더**: `st.header()` - 중간 크기
- **서브섹션**: `st.subheader()` - 작은 크기
- **본문**: 기본 텍스트
- **캡션**: `st.caption()` - 회색 작은 글씨

### 4.2 숫자 포맷팅
- **금액**: `1,234,567원` (천 단위 콤마)
- **수익률**: `+5.2%` (부호 포함, 소수점 1자리)
- **수량**: `1.2345주` (소수점 4자리)

## 5. 반응형 레이아웃

### 5.1 컬럼 구조
- **4컬럼 (Metric Cards)**: `st.columns(4)`
- **2컬럼 (차트 + 테이블)**: `st.columns([2, 1])`
- **3컬럼 (상위 종목)**: `st.columns(3)`

### 5.2 모바일 대응
- Streamlit은 자동으로 모바일 최적화
- 단, 컬럼이 너무 많으면 가독성 저하
- 권장: 최대 4컬럼까지만 사용

## 6. 인터랙션

### 6.1 사용자 입력
- **드롭다운**: 월 선택, 계좌 선택
- **토글**: ETF 투시 활성화/비활성화
- **텍스트 입력**: 종목 검색
- **슬라이더**: Top N 개수 조절 (선택사항)

### 6.2 피드백
- **로딩 인디케이터**: `st.spinner("데이터 로딩 중...")`
- **성공 메시지**: `st.success("데이터 로드 완료!")`
- **에러 메시지**: `st.error("데이터 로딩 실패")`
- **경고 메시지**: `st.warning("ETF 데이터가 부족합니다")`
- **정보 메시지**: `st.info("이 데이터는 캐시된 정보입니다")`

## 7. 레이아웃 구현 코드 스케치

```python
import streamlit as st

# 페이지 설정
st.set_page_config(
    page_title="포트폴리오 대시보드",
    page_icon="💰",
    layout="wide",  # 와이드 레이아웃
    initial_sidebar_state="expanded"
)

# 사이드바
with st.sidebar:
    st.title("📊 Navigation")
    page = st.radio(
        "페이지 선택",
        ["월별 투자 비교", "계좌별 포트폴리오", "전체 포트폴리오"]
    )

# 메인 헤더
st.title("💰 포트폴리오 대시보드")

# 월 선택 드롭다운
col1, col2, col3 = st.columns([2, 1, 1])
with col1:
    months = get_available_months()
    selected_month = st.selectbox(
        "📅 분석 월 선택",
        months,
        index=0
    )

# 구분선
st.divider()

# 메인 콘텐츠 (페이지별 라우팅)
if page == "월별 투자 비교":
    render_monthly_comparison(selected_month)
elif page == "계좌별 포트폴리오":
    render_account_portfolio(selected_month)
else:
    render_total_portfolio(selected_month)
```

## 8. 다음 단계

각 페이지별 상세 UI 디자인은 다음 문서를 참고하세요:
- `01-monthly-comparison.md`: 월별 투자 비교 페이지
- `02-account-portfolio.md`: 계좌별 포트폴리오 페이지
- `03-total-portfolio.md`: 전체 포트폴리오 페이지
